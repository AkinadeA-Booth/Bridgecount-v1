<!DOCTYPE html>
<html>
  <head>
    <title>FinalProject</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

  </head>
  <nav>
    <ul>
      <% if @current_user == nil %>
        <li>
          <a href="/user_sign_up">Sign up</a>
        </li>
        <li>
          <a href="/user_sign_in">Sign in</a>
        </li>

        <li>
          <a href="/companies">Companies</a>
        </li>
        <li>
          <a href="/users_leveraging_accepted_offers">Offers being used</a>
        </li>
        <li>
          <a href="/offers">All offers being used</a>
        </li>
      </li>

    <% else %>
      <li>
        <a href="/accepted_offers/:path_id"><%= @current_user.first_name%>'s offers</a>
      </li>
      <li>
        <a href="/user_sign_out">Sign out</a>
      </li>

    <% end %>

  </ul>
</nav>

<p style="color: green"><%= notice %></p>

<p style="color: red"><%= alert %></p>

<body>
  <%= yield %>
</body>
</html>
